<view class="comment_wrap">
    <van-image round height="30" width="30" fit="cover" src="{{userImg}}"/>
    <view class="right_wrap">
        <view class="user_name">{{userName}}</view>
        <view class="cm_text">
            {{text}}
        </view>
    </view>
</view>