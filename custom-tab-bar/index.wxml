<van-tabbar active="{{ active }}" bind:change="onChange" safe fixed active-color="rgba(71, 74, 217, 1)">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
